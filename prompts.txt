[
    {
        "text": "a function that sorts an array using the insertion sort algorithm",
        "code":
"def insertionSort(arr):\n    for i in range(1, len(arr)):\n        key = arr[i]\n        j = i\n        while j >=0 and key < arr[j] :\n                arr[j+1] = arr[j]\n                j -= 1\n        arr[j+1] = key"
    },
    {
        "text": "a function that goes through an array, printing 'fizz' if the array element is divisible by 3, 'buzz' if the element is divisible by 5, and 'fizzbuzz' if the element is divisible by 15",
        "code": "def fizzbuzz(n):\n    if n % 3 == 0 and n % 5 == 0:\n        return 'FizzBuzz'\n    elif n % 3 == 0:\n        return 'Fizz'\n    elif n % 5 == 0:\n        return 'Buzz'\n    else:\n        return str(n)"
    },
    {
        "text": "a function that generates the factorial of a given number",
        "code": "def factorial(x):\n    result = 1\n    for i in range(1, x+1):\n       result *= x\n   return result"
    },
    {
        "text": "a function that returns its argument",
        "code": "def identity(x):\n return x"
    }
]
